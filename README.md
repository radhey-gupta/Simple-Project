# Simple Node.js Hello World

This repository now contains a minimal Node.js "Hello, world!" project.

How to run:

1. Ensure you have Node.js installed (v12+).
2. From the repository root, run:

   npm install
   npm start

or:

   node index.js

The server listens on port 3000 by default and responds with "Hello, world!".
